# numpy==1.26.4 / scipy==1.11.4 were the original pins, but the pinned
# TrackEval SHA (12c8791b) uses removed numpy aliases (np.float, np.int) in
# its core MOT scoring path (mot_challenge_2d_box.py, hota.py, identity.py).
# Those aliases were removed in numpy>=1.24, so 1.26.4 crashes with
# AttributeError. Verified empirically; see README.md.
numpy==1.23.5
scipy==1.10.1
# For the vendored reference trackers (bytetrack_sweep.py). lapx provides
# `import lap` (lap itself does not build here); filterpy is OC-SORT's
# Kalman dep. CAUTION: `pip install filterpy` OUTSIDE this requirements
# file pulls latest numpy and breaks the 1.23.5 pin above — always install
# via `pip install -r requirements.txt` so the resolver holds the pin.
lapx==0.9.4
filterpy==1.4.5
